import * as React from 'react';
import { IOBadge } from "../../io-app/ts/components/core/IOBadge";

export const TestComponent = () => {
    return (       
        <IOBadge variant='outline' color='blue' text="Test"/>
    );
}